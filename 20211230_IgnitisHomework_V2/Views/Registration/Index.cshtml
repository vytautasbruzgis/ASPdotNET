@using _20211230_IgnitisHomework_V2.Models.Dtos
@model RegistrationViewDto
Works

@using (Html.BeginForm("Create", "RegistrationAttribute", FormMethod.Post))
{
    <table class="table">
        <thead>
            <tr scope = "row" class="">
              <th scope="col">Registracijos požymiai</th>
              <th scope="col">Reikšmė</th>
            </tr>
        </thead>
        @if (@Model.Registration.Attributes.Any())
        {
            <tbody>
            @for(int i = 0; i < @Model.Registration.Attributes.Count(); i++)
            {
                <tr scope = "row">
                <td scope="col">@Model.Registration.Attributes[i].Attribute.Name</td>
                <td scope="col">
                    @Html.DropDownListFor(a => a.Registration.Attributes[i].SelectedOptionId, new SelectList(@Model.Registration.Attributes[i].Attribute.Options, "Id", "Name"), "", new { @class = "form-control", disabled = "disabled" })    
                </td>
                </tr>
            }
        @*
            
            <tr scope = "row">
              <td scope="col">Rangos darbus atliks</td>
              <td scope="col">           
                    @Html.DropDownListFor(a => a.Registrationattributes.ContractorTypeId, new SelectList(@Model.ContractorTypes, "Id", "Name"), "", new { @class = "form-control", disabled = "disabled" })
              </td>
            </tr>
            <tr scope = "row">
              <td scope="col">Verslo klientas</td>
              <td scope="col">
                    @Html.DropDownListFor(a => a.Registrationattributes.IsBusinessClient, new [] { new SelectListItem { Text = "Taip", Value="true" } , new SelectListItem { Text = "Ne", Value="false"} }, "", new { @class = "form-control", disabled = "disabled" })    
              </td>
            </tr>
            <tr scope = "row">
              <td scope="col">Skaičiavimo metodas</td>
              <td scope="col">
                  @Html.DropDownListFor(a => a.Registrationattributes.CalculationTypeId, new SelectList(@Model.CalculationTypes, "Id", "Name"), "", new { @class = "form-control", disabled = "disabled" })
              </td>
            </tr>
            <tr scope = "row">
              <td scope="col">Svarbus klientas</td>
              <td scope="col">
                  @Html.DropDownListFor(a => a.Registrationattributes.CustomerImportanceId, new SelectList(@Model.CustomerImportances, "Id", "Name"), "", new { @class = "form-control", disabled = "disabled" })
              </td>
            </tr>
        *@
        </tbody>
        }
        
        
    </table>
    <br />   
    <input type="submit" value="Submit"/>
}

  
  